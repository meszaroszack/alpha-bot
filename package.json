{
  "name": "alpha-bot",
  "version": "1.0.0",
  "description": "Kalshi BTC Alpha Trading Bot - full-stack monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "cd backend && node server.js",
    "build": "npm run install:all && cd frontend && npm run build && cd .. && node scripts/copy-dist.js",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
